{
  "name": "book-shelf",
  "version": "1.0.0",
  "scripts": {
    "server": "json-server --watch db.json --port 3000 --host 0.0.0.0",
    "build": "npx @tailwindcss/cli -i ./index.css -o ./src/styles.css",
    "watch": "npx @tailwindcss/cli -i ./index.css -o ./src/styles.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.10",
    "tailwindcss": "^4.1.10"
  },
  "dependencies": {
    "json": "^11.0.0",
    "json-server": "^1.0.0-beta.3",
    "lucide": "^0.515.0",
    "server": "^1.0.41"
  }
}
